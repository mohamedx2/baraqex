{
  "name": "baraqex-fullstack-app",
  "version": "1.0.0",
  "type": "module",
  "description": "Full-stack Baraqex application with API and database",
  "main": "server.js",
  "scripts": {
    "dev": "node server.js",
    "build": "npm run build:server && npm run build:client",
    "build:server": "echo 'Building server...'",
    "build:client": "echo 'Building client...'",
    "start": "NODE_ENV=production node server.js",
    "test": "node test.js",
    "migrate": "node scripts/migrate.js"
  },
  "dependencies": {
    "baraqex": "^1.0.22",
    "bcryptjs": "^2.4.3",
    "express": "^4.18.2",
    "frontend-hamroun": "^1.2.35",
    "jsonwebtoken": "^9.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
