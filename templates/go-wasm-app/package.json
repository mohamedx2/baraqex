{
  "name": "baraqex-go-wasm-app",
  "version": "1.0.0",
  "type": "module",
  "description": "Baraqex application with Go WebAssembly integration",
  "main": "server.js",
  "scripts": {
    "dev": "npm run build:wasm && node server.js",
    "build": "npm run build:wasm && npm run build:server && npm run build:client",
    "build:wasm": "cd src/wasm && GOOS=js GOARCH=wasm go build -o ../../public/app.wasm main.go",
    "build:server": "echo 'Building server...'",
    "build:client": "echo 'Building client...'",
    "start": "NODE_ENV=production node server.js",
    "test": "node test.js",
    "test:wasm": "node test-wasm.js"
  },
  "dependencies": {
    "baraqex": "^1.0.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
